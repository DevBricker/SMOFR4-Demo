{
  "name": "ct-appointment-azure-functions",
  "version": "1.0.0",
  "description": "Azure Functions service simulating hospital CT appointment booking with Teams Adaptive Card notifications",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "jest --testEnvironment node",
    "test:coverage": "jest --coverage --testEnvironment node",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
